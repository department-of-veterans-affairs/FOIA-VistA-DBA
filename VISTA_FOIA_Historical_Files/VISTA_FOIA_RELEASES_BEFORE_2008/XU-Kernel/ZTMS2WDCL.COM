$!----------------------------------------------------------------------- $! ZTMS2WDCL.COM - Cache Start Submanager with a DCL Context$! * KERNEL 8 *$! p1 is the Cache config name$! p2 is the namespace to start.$! p3 is NOT used. (VOL for DSM)$!$! This file is submitted to the queue to run and it$!  builds and runs the TMP_pid file$!$! Build the file to run, can't pass arguments with RUN$ pid = F$GETJPI("","PID")$ infile = "TMP_" + pid + ".ZTMS"$ outfile = "TMP_" + pid + ".log"$ SAY = "write output"$!$! open and build the input file$ OPEN/write output 'infile'$ SAY "$! Taskman temp file to run a submanager"$ SAY "$! Delete this file if it is not open."$ SAY "$ set verify"$ SAY "$! ''P1' and ''P2'"$ SAY "$ csession ""''p1'"" ""-U"" ""''p2'"" ""START^%ZTMS"""$ SAY "$ exit"$ Close output$!$! If a log file is needed change _NLA0: to 'outfile$ name = "ZTMS_" + pid$  run sys$system:loginout.exe -       /input='infile -       /output=_NLA0: -       /detach /process='name$!$! 	Wait for loginout to run it then delete the file.$ wait 00:01$!$ del TMP_*.ZTMS;1$ exit